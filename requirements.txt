# Web Framework
Django==5.2.1
whitenoise==6.9.0
gunicorn==21.2.0  # Required for WSGI server in production

# Database
psycopg2-binary==2.9.10
supabase==2.15.1

# Vercel-specific requirements
vercel==0.31.1  # Optional but helpful for Vercel deployments
python-dotenv==1.0.1  # For environment variable management

# Additional recommended packages for production
Pillow==10.3.0  # If you're handling images
django-environ==0.11.2  # Better environment variable handling
